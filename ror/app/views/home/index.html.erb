<% if user_signed_in? %>

  <%else %>
  <div class="space-y-4 p-4 bg-gray-100 rounded-md shadow-md">
    <p>
    <%= link_to "Sign up", new_user_registration_path,  class: "text-blue-500 hover:text-blue-700 underline block" %>
  </p>
  <p>
    <%= link_to "Log in", new_user_session_path,  class: "text-blue-500 hover:text-blue-700 underline block" %>
  </p>
  </div>
  <%end %>